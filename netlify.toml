[build]
  functions = "netlify/functions"

[build.environment]
  PYTHON_VERSION = "3.11"

# Route every request through the Flask function.
[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/api"
  status = 200
  force  = true
